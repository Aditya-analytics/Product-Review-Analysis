[phases.setup]
aptPkgs = ['...', 'nodejs', 'npm']

[phases.install]
cmds = [
    'pip install -r requirements.txt',
    'cd frontend && npm ci'
]

[phases.build]
cmds = ['cd frontend && npm run build']

[start]
cmd = 'gunicorn api:app'
